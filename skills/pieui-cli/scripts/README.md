# pieui-cli scripts

Helper automation scripts for the `pieui` CLI skill. None yet — add shell/TypeScript helpers
here (e.g. `bun install` setup, bulk scaffolding, CI integration) as the need arises.

Repo-level tooling lives at the root: `scripts/check-cheatsheet-sync.sh` (advisory cheatsheet
drift check; see `TESTING.md` T8-00).
