# pieui-cli scripts

Helper automation scripts for the `pieui` CLI skill.

Place any shell or TypeScript helper scripts here (e.g. bulk scaffolding, CI integration).

## Examples of scripts to add

- `setup.sh` — verify Bun is installed and run `bun install`
- `batch-push.sh` — push all local cards to remote storage
